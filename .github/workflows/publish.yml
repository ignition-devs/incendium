name: publish

on:
  release:
    types:
      - published

jobs:
  main:
    uses: coatl-dev/workflows/.github/workflows/pypi-upload.yml@v2.1.0
    secrets:
      pypi-token: ${{ secrets.PYPI_API_TOKEN }}
