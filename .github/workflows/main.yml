name: main

on:
  workflow_dispatch:
  pull_request:
  push:
    branches:
      - main

jobs:
  test:
    uses: thecesrom/workflows/.github/workflows/tox.yml@v0.0.1
    with:
      image: ubuntu-20.04
      pre-commit: true
