name: main

on:
  workflow_dispatch:
  pull_request:
  push:
    branches:
      - main

jobs:
  tox:
    uses: coatl-dev/workflows/.github/workflows/tox-docker.yml@v5
    with:
      pre-commit: true
