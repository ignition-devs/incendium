[build-system]
build-backend = "setuptools.build_meta"
requires = [
  "setuptools>=42",
  "wheel",
]

[tool.black]
line-length = 79

[tool.isort]
line_length= 79
profile = "black"
py_version = 27

[tool.pydocstyle]
convention = "google"
